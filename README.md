# ConBrioMusica
Wrapper for yt-dlp.exe, and youtube scraper to populate meta tags for music files

This project uses Selenium, so chromedriver.exe needs to be in the Debug or Release folder.

Install ffmpeg to C:\ffmpeg, then copy yt-dlp.exe to C:\ffmpeg\bin